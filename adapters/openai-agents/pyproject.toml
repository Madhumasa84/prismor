[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prismor-openai"
version = "0.2.0"
description = "Prismor adapter for the OpenAI Agents SDK — per-user runtime tool-call policy for production agents."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Prismor" }]
keywords = ["openai", "agents", "security", "policy", "guardrail", "prismor", "prismor"]

# Requires the Prismor runtime (the `prismor` package) to be importable.
# It provides the shared policy engine + evaluate_tool_call entrypoint.
dependencies = [
    "prismor>=1.13.0",
]

[project.optional-dependencies]
# The OpenAI Agents SDK itself — only needed to actually run an Agent; the
# adapter wraps plain callables without it (so it is unit-testable standalone).
sdk = ["openai-agents>=0.0.1"]

[tool.hatch.build.targets.wheel]
packages = ["prismor_openai", "prismor"]
